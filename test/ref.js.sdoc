Caterwaul fn, bind, and cond standard library tests

test(function () {
  var fn = caterwaul.clone('std.ref');
  fn.foo = 'bar';

  fn(function (eq) {
    eq(this[caterwaul].foo, 'bar');
    this[caterwaul].foo = 'bif';
  }) (eq);

  eq(fn.foo, 'bif');
});
