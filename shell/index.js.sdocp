sdocp('sdoc::js::index', '$(caterwaul.clone(\'std seq continuation\')(function () {\n  $(\'body\').append(shell());\n  setTimeout(fn_[$(\'.prompt .input\').focus()], 10);\n\n  adjust_sizing(), $(window).resize(adjust_sizing),\n  where*[adjust_sizing() = $(\'body\').css({height: $(window).height()})];\n}));');