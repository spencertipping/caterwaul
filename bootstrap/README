Scripts to bootstrap the 1.4 compiler (which is self-hosting) from waul-1.3. You
won't need to use this because the 1.4 compiler can compile itself. I need it
because at the time of this writing the 1.4 compiler doesn't exist.
